# Vercel（またはローカル）の環境変数の例。
# ダッシュボード: Project → Settings → Environment Variables に同じ名前で登録してください。

# 必須: Google AI / Gemini（サーバー側のみ。ブラウザに置かない）
GEMINI_API_KEY=

# 任意: 未設定時は api/gemini.js と api/health-advice.js の既定モデル名を使用
# GEMINI_MODEL=gemini-2.0-flash
